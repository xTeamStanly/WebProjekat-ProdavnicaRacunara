export default class Configuration {
    constructor(ID, Name, CPU, GPU, RAM, MB, STORAGE) {
        this.ID = ID;
        this.Name = name;
        this.CPU = CPU;
        this.GPU = GPU;
        this.RAM = RAM;
        this.MB = MB;
        this.STORAGE = STORAGE;
    }
}